{
  "name": "afk-bypasser",
  "version": "1.0.0",
  "description": "Modern URL bypass tool",
  "scripts": {
    "build": "mkdir -p public && cp index.html public/ && cp supported.html public/ && cp afk-bypasser.user.js public/",
    "dev": "python3 -m http.server 3000"
  },
  "keywords": ["bypass", "url", "shortener"],
  "author": "AFK Bypasser",
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {}
}
